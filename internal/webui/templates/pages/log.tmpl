{{define "content"}}
{{$c := .Content}}
{{template "mrnav" (navargs $c.Nav "")}}
<section class="panel">
  <h2>Review log <span class="subtle">{{$c.Name}}</span></h2>
  <div class="agent-strip" id="agent-strip" hidden></div>
  <pre class="runlog" id="runlog">{{$c.Text}}</pre>
</section>
{{end}}
